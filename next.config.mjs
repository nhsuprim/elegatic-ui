/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["cdn.dummyjson.com", "example.com", "res.cloudinary.com"], // Add the domain here
    },
};

export default nextConfig;
